POSTGRES_PASSWORD=replace-with-a-long-random-password
CASEFLOW_ADMIN_TOKEN=replace-with-a-long-random-operator-token
ANALYZER_MODE=rules
PUBLIC_DEMO_MODE=0
WORKER_POLL_SECONDS=2

# Leave blank for the local synthetic CRM adapter. A real integration endpoint
# must be authorized and idempotent before setting these values.
CRM_WEBHOOK_URL=
CRM_WEBHOOK_TOKEN=
